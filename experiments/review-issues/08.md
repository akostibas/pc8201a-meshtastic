Tier B review checklist for **Chapter 8: RAM File Handling** (`docs/chapters/08-ram-file-handling.md`).

Auto-generated by `bin/gen-review-issues.sh` from in-file markers.
Re-run the script to refresh. Each item links to its source line.
Verify every value against the original scan — OCR'd numbers are not authoritative.

## Diagrams (figure → mermaid / table / image)
- [ ] FIGURE 8.1: Memory map showing loaded CO file layout (AXFFFF at top, CO files, DO files, BA files, AX8000 at bottom) — needs vision re-OCR from source page 119 (target: mermaid) <sub>(L219)</sub>
- [ ] FIGURE 8.2: Memory map showing state after machine program loaded/running (PC and STREND pointers shown) — needs vision re-OCR from source page 119 (target: mermaid) <sub>(L259)</sub>
- [ ] FIGURE 8.3: Memory map showing state after CO file deleted and data between STREND and XAAAA moved — needs vision re-OCR from source page 120 (target: mermaid) <sub>(L285)</sub>
- [ ] FIGURE 8.4: MAKHOL flow diagram — needs vision re-OCR from source page 130 (target: mermaid) <sub>(L401)</sub>
- [ ] FIGURE 8.5: LNKFIL internal flow diagram — needs vision re-OCR from source pages 130–131 (target: mermaid) <sub>(L449)</sub>

---
_Pipeline + per-figure-type policy: see `experiments/diagram-ocr/RESULTS.md`._
