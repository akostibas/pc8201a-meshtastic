Tier B review checklist for **Chapter 9: LCD Interface** (`docs/chapters/09-lcd-interface.md`).

Auto-generated by `bin/gen-review-issues.sh` from in-file markers.
Re-run the script to refresh. Each item links to its source line.
Verify every value against the original scan — OCR'd numbers are not authoritative.

## Diagrams (figure → mermaid / table / image)
- [ ] FIGURE 9.1: LCD block layout showing B1–B10 arranged in two rows across 240 dots × 64 dots — needs vision re-OCR from source page 155 (target: mermaid or table) <sub>(L11)</sub>
- [ ] FIGURE 9.2: PAGE/OFFSET Display RAM map showing 4 pages (address bits 'B00/'B01/'B10/'B11) each spanning OFFSET 0–49 (0–39 for B5/B10) — needs vision re-OCR from source page 157 (target: mermaid or table) <sub>(L98)</sub>
- [ ] FIGURE 9.3: LCD Block page-mapping diagram showing Starting Page=2 mapping — needs vision re-OCR from source page 159 (target: mermaid or table) <sub>(L150)</sub>
- [ ] FIGURE 9.4: Character dot-matrix diagram showing DATA Pattern (6 columns × 8 rows bit table) alongside Font Pattern (* dot display) for character "A" — needs vision re-OCR from source page 175 (target: markdown table or ASCII grid) <sub>(L615)</sub>

## Tables / values to verify
- [ ] verify register bit table against source page 156 <sub>(L42)</sub>
- [ ] verify register bit table against source page 156 <sub>(L63)</sub>
- [ ] verify register bit table against source page 157 <sub>(L79)</sub>
- [ ] verify register bit table against source page 158 <sub>(L131)</sub>
- [ ] verify register bit table against source page 160 <sub>(L176)</sub>
- [ ] verify register bit table against source page 160 <sub>(L191)</sub>
- [ ] verify register bit table against source page 161 <sub>(L218)</sub>
- [ ] The character code of the character displayed at location (1,1) on the LCD display is stored at `'XF000`, and the code of the character at (2,1) is stored at `'XFBC1`, and so on. So the code of the left-lowest character, (40,8), is stored at `'XFCBF`<!-- TODO(tier-b): verify this address — source says 'XFC3F which may conflict with the 1st-region end 'XFCFF; re-check source page 182 -->. This rule is used in the standard program in ROM #0. For instance, BASIC, TEXT and TELCOM use that area like a VRAM in the traditional desk-top personal computer. The menu screen also utilizes that area. But you can use this area as you like. The data in this area does not affect the information on the LCD display, as long as you use your own display routine. <sub>(L720)</sub>
- [ ] verify attribute table against source page 183 <sub>(L728)</sub>

---
_Pipeline + per-figure-type policy: see `experiments/diagram-ocr/RESULTS.md`._
