Tier B review checklist for **Chapter 4: How To Use 2nd/3rd RAM** (`docs/chapters/04-how-to-use-2nd-3rd-ram.md`).

Auto-generated by `bin/gen-review-issues.sh` from in-file markers.
Re-run the script to refresh. Each item links to its source line.
Verify every value against the original scan — OCR'd numbers are not authoritative.

## Diagrams (figure → mermaid / table / image)
- [ ] FIGURE 4.2: Memory map — code in RAM #1, data in RAM #2 (upper address scenario, step 1) — needs vision re-OCR from source page 52 (target: mermaid or table) <sub>(L68)</sub>
- [ ] FIGURE 4.3: Memory map — after bank switch, code in RAM #1 accesses RAM #2 (upper address scenario, step 2) — needs vision re-OCR from source page 53 (target: mermaid or table) <sub>(L85)</sub>
- [ ] FIGURE 4.4: Memory map — code in 2nd ROM running with RAM #1 (lower address scenario, step 1) — needs vision re-OCR from source page 54 (target: mermaid or table) <sub>(L118)</sub>
- [ ] FIGURE 4.5: Memory map — RAM #2 switched in during bank switch (lower address scenario, step 2) — needs vision re-OCR from source page 55 (target: mermaid or table) <sub>(L139)</sub>

---
_Pipeline + per-figure-type policy: see `experiments/diagram-ocr/RESULTS.md`._
