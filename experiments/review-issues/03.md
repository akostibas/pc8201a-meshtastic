Tier B review checklist for **Chapter 3: How To Use 2nd ROM** (`docs/chapters/03-how-to-use-2nd-rom.md`).

Auto-generated by `bin/gen-review-issues.sh` from in-file markers.
Re-run the script to refresh. Each item links to its source line.
Verify every value against the original scan — OCR'd numbers are not authoritative.

## Diagrams (figure → mermaid / table / image)
- [ ] FIGURE 3.1: Stack diagram showing stacked items before HLT — needs vision re-OCR from source page ~40 (target: mermaid) <sub>(L277)</sub>
- [ ] FIGURE 3.2: Control-flow diagram for 2nd ROM power-on/power-off/initialize/return sequence — needs vision re-OCR from source page ~41 (target: mermaid) <sub>(L298)</sub>

## Tables / values to verify
- [ ] verify interrupt vector table layout against source page ~30 <sub>(L19)</sub>
- [ ] verify interrupt hook table layout against source page ~30 <sub>(L32)</sub>
- [ ] verify port bit-assignment table against source page ~31 <sub>(L60)</sub>
- [ ] verify UART control port bit-assignment table against source page ~32 <sub>(L98)</sub>
- [ ] verify UART data port bit-assignment table against source page ~32 <sub>(L116)</sub>
- [ ] verify calendar clock port bit-assignment table against source page ~33 <sub>(L137)</sub>

---
_Pipeline + per-figure-type policy: see `experiments/diagram-ocr/RESULTS.md`._
