# claude-sonnet-4-6 — p130 (Fig 8.5 Internal flow of LNKFIL, flowchart)

## 1. Faithful transcription (boxes top→bottom)
1. `LNKFIL` (entry, rounded)
2. Mark the all valid directory flag (turn 0 bit of all valid directory flag)
3. Get the lowest file address
4. Get the lowest link pointer in the valid file's directory
5. Save this link pointer
6. Search the lowest link pointer in the marked files in directory area
7. Save the saved link pointer at this marked files link pointer field
8. Demark the directory flag of that file. (turn off the bit 0 of that file)

Off-page connectors at bottom: `(A)` left, `(B)` right.

## 2. Mermaid translation

```mermaid
flowchart TD
    ENTRY([LNKFIL])
    B1["Mark the all valid directory flag\n(turn 0 bit of all valid directory flag)"]
    B2["Get the lowest file address"]
    B3["Get the lowest link pointer\nin the valid file's directory"]
    B4["Save this link pointer"]
    B5["Search the lowest link pointer\nin the marked files in directory area"]
    B6["Save the saved link pointer\nat this marked files link pointer field"]
    B7["Demark the directory flag of that file.\n(turn off the bit 0 of that file)"]
    A(["(A)"])
    B(["(B)"])

    ENTRY --> B1 --> B2 --> B3 --> B4 --> B5 --> B6 --> B7 --> A
    B(["(B)"]) --> B5
```

CORRECTION (human-verified against source): sonnet originally produced a
`B4 -.->|loop back| B5` edge and treated `(B)` as an outbound connector from B7.
Both wrong. The `|<---` rail is the **inbound** connector `(B)`: it comes up the
right side and re-enters the main column at the junction between "Save this link
pointer" (B4) and "Search the lowest link pointer" (B5) — i.e. `(B) --> B5`.
`(A)` is the forward exit off the bottom (`B7 --> A`). This is exactly the kind
of edge-direction error the per-figure human review must catch — vision models
read box text reliably but get connector topology wrong.
